const initialFavoritesState = {
  favoritesProducts: [],
};

export const favoritesReducer = (state = initialFavoritesState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

import {Logo} from "./Logo";
import {StyledHeader} from "./styled/StyledHeader";

export const Header = () => {
    return (
        <StyledHeader>
            <header>
                <Logo/>
                <div>

                </div>
            </header>
        </StyledHeader>)
};

